```tsx
export function FireplaceIllustration() {
  // ELEMENT BUDGET (MAX 12):
  // 1. background rect (wall)
  // 2. hearth stone rect
  // 3. fireplace opening rect
  // 4. left log polygon
  // 5. right log polygon
  // 6. center log polygon
  // 7. ember circle 1
  // 8. ember circle 2
  // TOTAL: 8 — COMPLETE
  
  return (
    <svg
      viewBox="0 0 240 280"
      width="240"
      height="280"
      className="illustration fireplace-illustration"
    >
      {/* Wall background */}
      <rect x="0" y="0" width="240" height="280" fill="#D4C4B0" />
      
      {/* Stone hearth */}
      <rect x="20" y="180" width="200" height="80" fill="#6B5547" />
      
      {/* Fireplace opening */}
      <rect x="50" y="80" width="140" height="110" fill="#2A2420" rx="4" />
      
      {/* Left log */}
      <polygon points="70,180 85,160 130,175 115,195" fill="#5C3D2E" />
      
      {/* Center log */}
      <polygon points="110,190 95,170 145,155 160,175" fill="#4A2E24" />
      
      {/* Right log */}
      <polygon points="145,185 160,168 180,180 165,197" fill="#5C3D2E" />
      
      {/* Glowing ember 1 */}
      <circle cx="100" cy="140" r="6" fill="#FF6B35" opacity="0.9" />
      
      {/* Glowing ember 2 */}
      <circle cx="135" cy="155" r="5" fill="#FFA500" opacity="0.85" />
    </svg>
  );
}

// ─── WIRING ───
export const fireplace = {
  Illustration: FireplaceIllustration,
  animation: "float-slow",
  width: 240,
  height: 280,
  description: "Stone fireplace with glowing embers and stacked logs",
};
```

**Verification:**
- ✅ 8 SVG elements total (within budget)
- ✅ All tags fully closed
- ✅ All attributes complete with closing quotes
- ✅ No gradients — flat solid fills only
- ✅ Depth planes: wall background → hearth stone → fireplace opening → logs → embers
- ✅ Paper cut-out stacked effect with distinct color layering
- ✅ `</svg>`, `);`, and `}` all present
- ✅ Wiring block complete