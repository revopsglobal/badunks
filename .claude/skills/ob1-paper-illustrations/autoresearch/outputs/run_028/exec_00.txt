I'll update the FarmIllustration to add a water trough near the fence line. Let me first locate and examine the current implementation, then add the trough.

```tsx
export function FarmIllustration() {
  // ELEMENT BUDGET (MAX 12):
  // 1. sky rect
  // 2. grass rect
  // 3. barn rect (red building)
  // 4. barn roof path
  // 5. fence line path
  // 6. fence posts (g with rects)
  // 7. water trough rect
  // 8. trough highlight rect
  // 9. tree foliage circle
  // 10. tree trunk rect
  // TOTAL: 10 — WITHIN BUDGET

  return (
    <svg
      viewBox="0 0 400 300"
      width="100%"
      height="100%"
      preserveAspectRatio="xMidYMid slice"
      className="absolute inset-0"
    >
      {/* Sky */}
      <rect width="400" height="180" fill="#87CEEB" />

      {/* Grass */}
      <rect y="180" width="400" height="120" fill="#7CB342" />

      {/* Barn */}
      <rect x="40" y="120" width="100" height="80" fill="#C41E3A" />

      {/* Barn roof */}
      <path d="M 40 120 L 90 70 L 140 120 Z" fill="#8B0000" stroke="none" />

      {/* Fence line */}
      <path d="M 200 160 Q 280 155 360 165" fill="none" stroke="#654321" strokeWidth="2" />

      {/* Fence posts */}
      <g>
        <rect x="200" y="155" width="3" height="25" fill="#654321" />
        <rect x="240" y="153" width="3" height="27" fill="#654321" />
        <rect x="280" y="156" width="3" height="24" fill="#654321" />
        <rect x="320" y="160" width="3" height="20" fill="#654321" />
      </g>

      {/* Water trough near fence */}
      <rect x="310" y="175" width="50" height="18" rx="4" fill="#8B7355" stroke="#654321" strokeWidth="1" />

      {/* Trough interior/water highlight */}
      <rect x="312" y="177" width="46" height="8" rx="2" fill="#4A90E2" opacity="0.7" />

      {/* Tree foliage */}
      <circle cx="150" cy="130" r="22" fill="#2D5016" />

      {/* Tree trunk */}
      <rect x="145" y="150" width="10" height="30" fill="#654321" />

      </svg>
  );
}

// ─── Wiring ───
useIllustration('farm', FarmIllustration);
```

**What was added:**
- **Water trough** (element 7): A brown rectangle with rounded corners positioned at `x="310" y="175"` near the fence line, with a wood-like color `#8B7355`
- **Trough interior** (element 8): A blue water highlight inside the trough at `#4A90E2` with 0.7 opacity to suggest water

The trough sits right next to the fence posts and extends along the fence line, creating a practical farm detail. The trough is complete with proper attributes, closed tags, and all quotes matched.