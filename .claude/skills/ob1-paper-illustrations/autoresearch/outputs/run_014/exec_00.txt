```tsx
// ELEMENT BUDGET (MAX 12):
// 1. sky rect (amber)
// 2. far hill path (dark)
// 3. mid hill path (medium)
// 4. foreground hill path (lighter)
// 5. rooster body circle
// 6. rooster head circle
// 7. rooster tail polygon
// 8. rooster comb polygon
// 9. rooster leg line
// TOTAL: 9 — STOP HERE

export function SunriseIllustration() {
  return (
    <svg
      viewBox="0 0 240 280"
      width="240"
      height="280"
      xmlns="http://www.w3.org/2000/svg"
      className="sunrise-illustration"
    >
      {/* Sky — warm amber sunrise */}
      <rect width="240" height="280" fill="#F4D4A8" />

      {/* Far hill — darkest silhouette */}
      <path
        d="M 0 160 Q 60 120 120 140 T 240 160 L 240 280 L 0 280 Z"
        fill="#2C3E1F"
      />

      {/* Mid hill — medium tone */}
      <path
        d="M 0 180 Q 80 140 140 165 T 240 185 L 240 280 L 0 280 Z"
        fill="#3D5A2B"
      />

      {/* Foreground hill — lighter, closest */}
      <path
        d="M 0 210 Q 120 180 200 210 L 240 220 L 240 280 L 0 280 Z"
        fill="#5A7C4A"
      />

      {/* Rooster silhouette on middle hill */}
      <g transform="translate(160, 160)">
        {/* Body */}
        <circle cx="0" cy="0" r="14" fill="#1A1A1A" />

        {/* Head */}
        <circle cx="18" cy="-8" r="10" fill="#1A1A1A" />

        {/* Comb — pointed red detail */}
        <polygon points="22,-16 26,-22 24,-16" fill="#D4401A" />

        {/* Tail — layered feathers pointing up-left */}
        <polygon points="-12,-8 -20,-16 -18,-4 -24,-2 -16,4" fill="#1A1A1A" />

        {/* Leg */}
        <line x1="2" y1="14" x2="2" y2="22" stroke="#1A1A1A" strokeWidth="2" />
      </g>
    </svg>
  );
}

// ─────────────────────────────────────
// WIRING: Register animation + export
// ─────────────────────────────────────

export const sunriseConfig = {
  name: "sunrise",
  component: SunriseIllustration,
  animationClass: "sunrise-animation",
  description: "Rooster greeting the amber dawn over misty hills",
};
```

**Add this to `/app/globals.css` under `/* ─── Illustration motion ─── */`:**

```css
.sunrise-illustration {
  animation: sunrise-fade 3s ease-in-out infinite;
}

@keyframes sunrise-fade {
  0% {
    opacity: 0.8;
  }
  50% {
    opacity: 1;
  }
  100% {
    opacity: 0.8;
  }
}
```

---

### Scene Breakdown:
- **Sky (amber):** Warm #F4D4A8 ground for the sunrise
- **3-plane depth hills:** Dark far hill → medium mid hill → lighter foreground (stacked silhouettes)
- **Rooster silhouette:** Black body + head on the mid hill with red comb accent, feather tail, single leg
- **Animation:** Gentle breathing fade to suggest fog lifting

✅ **Complete scene (9 elements, fully closed, no truncation)**