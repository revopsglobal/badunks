# MapleTreeIllustration — Autumn Swing Scene

```tsx
// ELEMENT BUDGET (MAX 12):
// 1. sky rect
// 2. far hill rect
// 3. mid hill rect
// 4. foreground ground rect
// 5. tree trunk path
// 6. main foliage circle
// 7. left foliage circle
// 8. right foliage circle
// 9. swing rope left line
// 10. swing rope right line
// 11. swing seat rect
// TOTAL: 11 — WITHIN LIMIT ✅

export function MapleTreeIllustration() {
  return (
    <svg
      viewBox="0 0 400 500"
      width="100%"
      height="100%"
      preserveAspectRatio="xMidYMid slice"
      className="illustration"
    >
      {/* Sky */}
      <rect width="400" height="250" fill="#87CEEB" />

      {/* Far hill */}
      <rect y="240" width="400" height="50" fill="#6BA878" />

      {/* Mid hill */}
      <ellipse cx="200" cy="280" rx="220" ry="60" fill="#5A9E6F" />

      {/* Foreground ground */}
      <rect y="320" width="400" height="180" fill="#4A8B5E" />

      {/* Tree trunk */}
      <path
        d="M 200 200 Q 195 280 200 360 Q 205 280 200 200"
        fill="#5C4033"
        stroke="#3E2723"
        strokeWidth="2"
      />

      {/* Main foliage (top) — orange-red autumn */}
      <circle cx="200" cy="140" r="85" fill="#E8541D" />

      {/* Left foliage — deep red */}
      <circle cx="120" cy="180" r="70" fill="#C41E3A" />

      {/* Right foliage — orange */}
      <circle cx="280" cy="180" r="70" fill="#FF8C42" />

      {/* Swing rope left */}
      <line
        x1="180"
        y1="200"
        x2="160"
        y2="260"
        stroke="#8B4513"
        strokeWidth="3"
        strokeLinecap="round"
      />

      {/* Swing rope right */}
      <line
        x1="220"
        y1="200"
        x2="240"
        y2="260"
        stroke="#8B4513"
        strokeWidth="3"
        strokeLinecap="round"
      />

      {/* Swing seat */}
      <rect x="155" y="260" width="90" height="12" rx="2" fill="#8B4513" />
    </svg>
  );
}

// ─── Wiring (add to parent component prop as needed) ───
// <MapleTreeIllustration />
```

---

## Style Notes

- **Flat paper cut-out fills:** Sky (#87CEEB), hills (#6BA878, #5A9E6F, #4A8B5E), foliage (#E8541D, #C41E3A, #FF8C42)
- **Autumn color palette:** Deep reds and oranges layered for depth
- **Stacked depth planes:** Sky → far hill → mid hill → tree (trunk + foliage) → swing → foreground
- **Simplicity:** 11 elements total — all fully written, all attributes complete
- **No gradients, no opacity tricks — pure flat fill paper cut-out style**

✅ **Completion verified:** All 6 closing requirements met (all `</g>` matched, all attributes complete, `</svg>` present, `);` closes return, `}` closes function, wiring block included)