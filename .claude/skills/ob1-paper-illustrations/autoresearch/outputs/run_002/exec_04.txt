```tsx
export function WorkshopIllustration() {
  const P = {
    sky: "#F5E6D3",
    wall: "#D4C4B0",
    wood: "#8B6F47",
    darkWood: "#5C4A36",
    pegboard: "#A89680",
    metal: "#7A8FA3",
    shadow: "#3D2817",
    accent: "#E8A959",
    sawdust: "#C4A574",
  };

  return (
    <svg viewBox="0 0 400 320" className="w-full h-full">
      <defs>
        <filter id="dropShadow" x="-50%" y="-50%" width="200%" height="200%">
          <feDropShadow dx="0" dy="3" stdDeviation="2" floodOpacity="0.4" />
        </filter>
        <filter id="glow" x="-50%" y="-50%" width="200%" height="200%">
          <feGaussianBlur stdDeviation="2" result="coloredBlur" />
          <feMerge>
            <feMergeNode in="coloredBlur" />
            <feMergeNode in="SourceGraphic" />
          </feMerge>
        </filter>
      </defs>

      {/* Back wall */}
      <rect width="400" height="320" fill={P.wall} />

      {/* Wall shading */}
      <rect x="0" y="240" width="400" height="80" fill={P.shadow} opacity="0.15" />

      {/* Pegboard back panel */}
      <rect x="40" y="60" width="280" height="160" fill={P.pegboard} rx="4" filter="url(#dropShadow)" />

      {/* Pegboard holes pattern */}
      {[...Array(8)].map((_, col) =>
        [...Array(6)].map((_, row) => (
          <circle
            key={`peg-${col}-${row}`}
            cx={60 + col * 38}
            cy={80 + row * 25}
            r="2.5"
            fill={P.wall}
            opacity="0.4"
          />
        ))
      )}

      {/* Workbench surface */}
      <rect x="30" y="220" width="340" height="80" fill={P.wood} rx="2" filter="url(#dropShadow)" />

      {/* Bench edge highlight */}
      <rect x="30" y="220" width="340" height="4" fill={P.accent} opacity="0.6" />

      {/* Bench shadow/depth */}
      <rect x="30" y="296" width="340" height="4" fill={P.darkWood} opacity="0.5" />

      {/* ─── TOOLS ON PEGBOARD ─── */}

      {/* Hammer (left side) */}
      <g style={{ animation: "swingTool 3s ease-in-out infinite", transformOrigin: "75px 70px" }}>
        <rect x="65" y="90" width="20" height="50" fill={P.wood} rx="2" />
        {/* Hammer head */}
        <rect x="60" y="85" width="30" height="16" fill={P.metal} rx="2" />
        <rect x="62" y="87" width="26" height="12" fill={P.shadow} opacity="0.3" />
      </g>

      {/* Wrench (center-left) */}
      <g style={{ animation: "spinTool 4s linear infinite", transformOrigin: "155px 115px" }}>
        <path d="M 145 115 Q 155 110 170 115 Q 175 125 170 130 Q 155 125 145 130 Z" fill={P.metal} />
        <circle cx="155" cy="122" r="4" fill={P.darkWood} />
      </g>

      {/* Screw