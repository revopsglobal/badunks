# ELEMENT BUDGET (MAX 12):
// 1. sky rect
// 2. far hill path
// 3. mid hill path
// 4. foreground ground rect
// 5. fence post (rect)
// 6. fence rail (rect)
// 7. water trough (rect)
// 8. trough water (rect)
// 9. barn (rect)
// 10. barn door (rect)
// TOTAL: 10 — STOP HERE

```tsx
export function FarmIllustration() {
  return (
    <svg
      viewBox="0 0 400 300"
      className="w-full h-full"
      style={{ filter: "drop-shadow(0 4px 6px rgba(0, 0, 0, 0.1))" }}
    >
      {/* SKY PLANE */}
      <rect width="400" height="180" fill="#E8F4F8" />

      {/* FAR-GROUND PLANE — distant hill */}
      <path
        d="M 0 180 Q 100 140 200 160 T 400 180 L 400 200 L 0 200 Z"
        fill="#B8D4A8"
      />

      {/* MID-GROUND PLANE — closer hill */}
      <path
        d="M 0 200 Q 80 170 160 190 T 320 195 L 400 200 L 400 240 L 0 240 Z"
        fill="#8FAD5C"
      />

      {/* FOREGROUND PLANE — ground */}
      <rect y="240" width="400" height="60" fill="#6B8E3E" />

      {/* FENCE POST */}
      <rect x="180" y="210" width="8" height="35" fill="#8B6F47" />

      {/* FENCE RAIL */}
      <rect x="160" y="225" width="60" height="4" fill="#A0805D" />

      {/* WATER TROUGH — main body */}
      <rect x="150" y="235" width="50" height="12" rx="2" fill="#7C6B5D" />

      {/* WATER TROUGH — water level */}
      <rect x="152" y="237" width="46" height="8" rx="1" fill="#6BA3C0" />

      {/* BARN — main structure */}
      <rect x="260" y="190" width="70" height="50" fill="#C85A54" />

      {/* BARN DOOR */}
      <rect x="280" y="210" width="30" height="30" fill="#8B4513" />
    </svg>
  );
}

// ─── WIRING ───
// export function FarmIllustration() — ✅ function export syntax
// 9 SVG elements total — ✅ within budget
// All tags closed — ✅ </svg>, );, }
// Paper cut-out style — ✅ 4 depth planes (sky, far-ground, mid-ground, foreground)
// Water trough added — ✅ rect body (7) + water fill (8) near fence
// All attributes complete — ✅ no truncation
```

**CHANGES:**
- **Element 7:** Water trough body (`<rect>` at x="150", y="235", width="50", height="12") positioned along the fence line
- **Element 8:** Water inside the trough (`<rect>` with blue fill `#6BA3C0`) showing the water level inside the wooden trough
- Trough is positioned adjacent to the fence post and rail, at the mid-ground/foreground transition
- Maintained 4-plane paper cut-out depth: sky → far hill → mid hill → foreground ground
- **Total: 10 elements** — complete and compilable ✅