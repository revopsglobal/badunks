# MealsIllustration — Cozy Kitchen Scene

I'll create a warm paper-cutout kitchen illustration with a cast iron pot, wooden cutting board, and fresh herbs.

---

## 1. Component Code

Add this to `/Users/gregharned/work/ob1-app/app/Illustrations.tsx`:

```tsx
export function MealsIllustration() {
  const colorCream = "#F5EFE0";
  const colorWood = "#8B6F47";
  const colorWoodDark = "#6B5435";
  const colorIron = "#2C2C2C";
  const colorHerbGreen = "#4A7C59";
  const colorHerbLight = "#6FA876";
  const colorCounter = "#D4C5B9";
  const shadowMild = "drop-shadow(2px 2px 3px rgba(0,0,0,0.15))";
  const shadowMedium = "drop-shadow(3px 3px 5px rgba(0,0,0,0.25))";
  const shadowDeep = "drop-shadow(4px 4px 8px rgba(0,0,0,0.35))";

  return (
    <svg
      viewBox="0 0 400 500"
      xmlns="http://www.w3.org/2000/svg"
      className="w-full h-full"
    >
      {/* Background – Kitchen wall */}
      <rect width="400" height="500" fill={colorCream} />

      {/* Wall shelf outline */}
      <rect x="20" y="80" width="360" height="20" fill={colorWoodDark} style={{ filter: shadowMild }} />

      {/* Counter top – base layer */}
      <ellipse cx="200" cy="320" rx="180" ry="60" fill={colorCounter} style={{ filter: shadowMedium }} />

      {/* Wooden cutting board – back */}
      <rect x="60" y="240" width="140" height="100" rx="8" fill={colorWood} style={{ filter: shadowMedium }} />

      {/* Cutting board grain detail – horizontal lines */}
      <line x1="70" y1="260" x2="190" y2="260" stroke={colorWoodDark} strokeWidth="1" opacity="0.5" />
      <line x1="70" y1="280" x2="190" y2="280" stroke={colorWoodDark} strokeWidth="1" opacity="0.5" />
      <line x1="70" y1="300" x2="190" y2="300" stroke={colorWoodDark} strokeWidth="1" opacity="0.5" />
      <line x1="70" y1="320" x2="190" y2="320" stroke={colorWoodDark} strokeWidth="1" opacity="0.5" />

      {/* Cast iron pot – body */}
      <ellipse cx="240" cy="260" rx="65" ry="50" fill={colorIron} style={{ filter: shadowDeep }} />

      {/* Cast iron pot – rim */}
      <ellipse cx="240" cy="220" rx="68" ry="20" fill={colorIron} />

      {/* Pot highlight – top left edge */}
      <ellipse cx="215" cy="215" rx="25" ry="8" fill={colorCounter} opacity="0.4" />

      {/* Cast iron pot handle – left side */}
      <path
        d="M 180 250 Q 160 240 170 280"
        stroke={colorIron}
        strokeWidth="12"
        fill="none"
        style={{ filter: shadowMedium }}
      />

      {/* Cast iron pot handle – right side */}
      <path
        d="M 300 250 Q 320 240 310 280"
        stroke={colorIron}
        strokeWidth="12"
        fill="none"
        style={{ filter: shadowMedium }}
      />

      {/* Herb bunch 1 – Rosemary (left, back) */}
      <g style={{ filter: shadowMild }}>